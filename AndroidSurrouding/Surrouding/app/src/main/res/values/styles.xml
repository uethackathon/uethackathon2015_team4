<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="windowActionBar">false</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/background_material_light</item>
        <item name="android:textColorPrimary">@color/color_primary_text</item>
        <item name="colorButtonNormal">@color/color_primary</item>
        <item name="android:textSize">16sp</item>
        <item name="android:actionMenuTextColor">@color/color_primary_text</item>

    </style>
    <style name="EditTextOnActionbar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColorHint">@color/color_disabled_text_dark_background</item>
        <item name="android:textColor">@color/color_secondary_text_dark_background</item>
        <item name="colorControlNormal">@color/color_secondary_text_dark_background</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="colorControlHighlight">@color/white</item>
    </style>


    <style name="AccentColorButtons" parent="AppTheme">
        <item name="colorButtonNormal">@color/color_accent</item>
        <item name="android:textColorPrimaryInverse">@color/color_primary_text_dark_background</item>
    </style>

    <style name="Widget.MyApp.Toolbar.Solid" parent="Widget.AppCompat.ActionBar">

    </style>

    <style name="ThemeOverlay.MyApp.ActionBar" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- Parent theme sets colorControlNormal to textColorPrimary. -->
        <item name="android:textColorPrimary">@color/mdtp_white</item>
    </style>
</resources>
